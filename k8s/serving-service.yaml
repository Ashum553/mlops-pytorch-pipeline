apiVersion: v1

kind: Service

metadata:
  name: cifar10-service
  namespace: mlops-cifar10

spec:
  type: ClusterIP

  selector:
    app: cifar10-serving

  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080

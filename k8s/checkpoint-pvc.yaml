apiVersion: v1

kind: PersistentVolumeClaim

metadata:
  name: cifar10-checkpoints
  namespace: mlops-cifar10

spec:
  accessModes:
    - ReadWriteOnce

  resources:
    requests:
      storage: 1Gi

  storageClassName: standard
